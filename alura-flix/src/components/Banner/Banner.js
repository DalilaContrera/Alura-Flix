import styles from "./Banner.module.css";

function Banner({color}){
    return(

        <div className={`${styles.gradient} ${styles['color-fondo']}`}>
          
        </div>


    )
}

export default Banner;